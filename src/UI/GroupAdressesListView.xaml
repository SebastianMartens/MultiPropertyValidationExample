<UserControl x:Class="MultiPropertyValidationExample.UI.GroupAdressListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="clr-namespace:MultiPropertyValidationExample.UI" xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      d:DataContext="{d:DesignInstance Type=ui:GroupAdressesViewModel}"
	>

    <!--  
    <DataGrid ItemsSource="{Binding GroupAdressCollectionView.View}" />
    -->
    <DataGrid ItemsSource="{Binding GroupAdressCollectionView.View}" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
            <DataGridTextColumn Header="Read GA" Binding="{Binding ReadAdress}"></DataGridTextColumn>
            <DataGridTextColumn Header="Write GA" Binding="{Binding WriteAdress}"></DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
   
            
</UserControl>
